{{ added.name }} ({{ added.crsid }}) has now been granted access to the '{{ target.society }}' shared account, at the request of {{ requester.name }} ({{ requester.crsid }}).

The complete list of administrators is as follows:

{{ admins }}
